import { AppLayout } from '@/components/appLayout';

export default function Home() {
	return (
		<AppLayout>
			<section className='w-full min-h-screen flex items-center justify-center'>Home Page</section>
		</AppLayout>
	);
}
