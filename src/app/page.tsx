import { AppLayout } from '@/components/appLayout';

export default function Home() {
	return (
		<AppLayout>
			<section className='w-full min-h-screen flex items-center justify-center text-black bg-blue-600'>Home Page</section>
		</AppLayout>
	);
}
